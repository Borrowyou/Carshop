CREATE TABLE PART_LINK
(
   LINK_ID INT NOT NULL PRIMARY KEY,
   PART_ID INT NULL, 
   URL VARCHAR(500),
   LAST_UPDATE DATETIME NULL,
   CONSTRAINT FK_PART_LINK_PART FOREIGN KEY (PART_ID) REFERENCES PARTS(PART_ID)
)